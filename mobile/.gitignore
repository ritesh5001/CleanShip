node_modules/
.expo/
dist/
web-build/
*.log

# Native projects are generated by `expo prebuild` / EAS and are not checked in.
/android
/ios

# Secrets and per-machine config.
.env
.env.local

# Signing keys. Back these up off the machine — losing release.keystore means
# never being able to update an installed app under the same package name.
/credentials
/build

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli