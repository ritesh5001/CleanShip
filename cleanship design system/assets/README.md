# Assets

| File | Notes |
| --- | --- |
| `image-slot.js` | Droppable image placeholder web component. Every `PhotoFrame` with no `src` renders one. |

**Missing on purpose:** no logo, no photography, no icon files — none were supplied, and nothing has been drawn or generated to stand in. Icons come from Lucide via CDN (flagged substitution, see `readme.md` → Iconography); the wordmark is typographic.
