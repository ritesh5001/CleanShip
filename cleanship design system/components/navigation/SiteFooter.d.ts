/**
 * Four-column navy footer with wordmark, services, official info and hours.
 * @startingPoint section="Navigation" subtitle="Navy four-column site footer" viewport="1280x420"
 */
export interface SiteFooterProps {
  services?: string[];
  style?: React.CSSProperties;
}
export declare function SiteFooter(props: SiteFooterProps): JSX.Element;
