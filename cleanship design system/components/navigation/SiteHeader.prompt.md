One-line: the site masthead — pair it under `<UtilityBar />`.

```jsx
<UtilityBar /><SiteHeader active="Services" onNavigate={setPage} />
```

No logo file was supplied, so the wordmark is typographic (Barlow Condensed, "Clean" navy + "ship" blue) beside a Lucide anchor. Replace with the real mark when it arrives.
