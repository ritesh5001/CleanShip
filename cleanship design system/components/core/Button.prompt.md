One-line: the site's action control — use for "Read more", "Get a quote", form submits; uppercase label, near-square corners, no rounded pills.

```jsx
<Button variant="primary" size="lg" iconRight={<Icon name="arrow-right" size={16} />}>Request a quote</Button>
```

Variants: primary (blue), navy, secondary (outline), ghost, onDark, onDarkOutline. Hover darkens to navy; press scales to .985. Never more than one primary per view region.
