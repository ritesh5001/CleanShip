One-line: the generic surface — border first, shadow only when interactive and hovered.

```jsx
<Card interactive accentRule><h4>IMO &amp; port-compliant procedures</h4><p>…</p></Card>
```

Tones: light (default), tint (blue-50 blocks), dark (navy sections).
