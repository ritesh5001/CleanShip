One-line: square icon-only button for social links, footer actions and slider arrows.

```jsx
<IconButton icon="linkedin" label="LinkedIn on" variant="onDark" size={44} />
```

Always pass `label`. Use `onDark` inside the navy footer, `outline` on white.
