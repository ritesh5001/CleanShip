One-line: uppercase micro-label for statuses ("IMO compliant", "24/7") — not for navigation.

```jsx
<Badge tone="accent">IMO compliant</Badge>
```

Default square corners; `shape="pill"` only inside dense lists.
