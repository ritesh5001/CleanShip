One-line: wraps a control with its uppercase micro-label and hint/error line. Input/Textarea/Select use it internally — reach for it directly only for custom controls.

```jsx
<Field label="Vessel type" hint="Bulk carrier, tanker, container…"><Input /></Field>
```
