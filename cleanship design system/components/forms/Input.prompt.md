One-line: the contact-form text field — 44px tall, 1px cool border, blue focus ring.

```jsx
<Input id="email" label="Mail address" placeholder="ops@yourfleet.com" iconLeft={<Icon name="mail" size={16} />} />
```
